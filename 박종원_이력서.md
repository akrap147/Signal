# 박종원 (Park Jong-won) 이력서

**대용량 트래픽 처리 역량을 갖춘 책임감 있는 백엔드 개발자**

---

## 💡 Introduction

### 지원 동기

Java 기반 웹 백엔드 기술을 중심으로 **대규모 트래픽 처리**를 고려한 실무형 프로젝트를 수행해 왔습니다.

- **Spring Boot 멀티 모듈 아키텍처**와 **RabbitMQ**를 활용한 분산 채팅 시스템(Discord Clone)을 설계하고 구현한 경험이 있습니다.
- **Redis 기반 메시지 순서 보장**, **WebSocket 실시간 통신**, **비동기 메시지 처리**를 통해 **초당 수천 건의 메시지를 안정적으로 처리**하는 구조를 익혔습니다.

단순한 CRUD 구현을 넘어 **확장 가능한 아키텍처 설계**와 **성능 최적화**에 깊은 관심을 가지고 있으며, 안정적이고 빠른 서비스를 제공하는 개발자로 성장하고자 합니다.

### 핵심 역량

- **성능 최적화**: Redis 기반 메시지 순서 보장 메커니즘 구현으로 처리량 60% 향상 (250 msg/s → 400 msg/s)
- **분산 시스템 설계**: RabbitMQ 기반 Event-Driven Architecture로 초당 5,000+ 메시지 처리 가능한 확장성 확보
- **데이터 최적화**: Redis 기반 캐시 처리, PostgreSQL/MySQL 데이터 모델링 및 Query 튜닝에 강점이 있습니다.
- **책임감 및 소통**: 팀 프로젝트 시 일정 준수를 최우선 가치로 여기며, 기획자 및 디자이너와 원활하게 소통하여 팀의 신뢰를 얻습니다.

---

## 📞 Personal Information

- **Name**: 박종원
- **Github**: https://github.com/akrap147
- **Blog**: https://velog.io/@akrap/posts
- **Portfolio**: https://github.com/akrap147/Signal
- **Certification**: 정보처리기사

---

## 🛠 Projects

### 1. SIGNAL - 대규모 동시 접속 환경을 고려한 실시간 커뮤니티 플랫폼

**일정**: 2024.05 ~ 2024.11 (1인 프로젝트)  
**기술 스택**: Java 17, Spring Boot 3.4, JPA, QueryDSL, PostgreSQL, Redis, RabbitMQ, WebSocket, Elasticsearch, Docker  
**서비스 개요**: Discord Clone - 대규모 동시 접속 환경을 고려한 실시간 채팅 및 음성 통화 플랫폼 개발

**GitHub**: https://github.com/akrap147/Signal

---

#### 📊 **Redis 기반 메시지 순서 보장 시스템 설계 및 성능 최적화**

- **문제**: 분산 환경에서 여러 서버가 동시에 메시지를 처리할 때 DB AUTO_INCREMENT로는 순서 보장 불가, RabbitMQ Consumer의 비동기 처리로 DB 저장 순서 뒤바뀜, 초당 수천 건 메시지 처리 시 DB 병목으로 응답 지연 (평균 10ms 이상)
- **해결**: Redis Atomic Increment(`INCR`)로 채팅방별 Global Sequence ID 발급 메커니즘 구현, Event-Driven Architecture 적용으로 실시간 전송(WebSocket)과 DB 저장(RabbitMQ) 분리, Producer-Consumer 패턴으로 메시지 전송과 영속화 비동기 처리
- **결과**: 메시지 순서 정확도 **100% 달성** (순서 오류 0건), DB AUTO_INCREMENT 대비 **처리량 60% 향상** (250→400 msg/s), 평균 응답시간 **50% 감소** (5.0ms→2.5ms), Python 부하 테스트로 **동시 접속자 500명, 10,000건 메시지** 처리 검증
- **기술 스택**: Redis Atomic Operations, RabbitMQ, WebSocket, STOMP, Event-Driven Architecture

---

#### 🔄 **RabbitMQ 기반 분산 메시징 아키텍처 및 멀티 모듈 설계**

- **문제**: 단일 서버 환경에서 메시지 처리량 한계 (초당 1,000건 이하), 서버 확장 시 메시지 중복 처리 및 순서 보장 문제, DB 저장 지연으로 실시간 채팅 성능 저하, API 서버와 채팅 서버 책임 분리 어려움
- **해결**: RabbitMQ STOMP Broker Relay로 Topic 기반 Pub/Sub 구성, ChatController(Producer)에서 메시지 수신 → Redis seqId 발급 → 실시간 전송 + RabbitMQ 발행, DbConsumer가 Queue에서 비동기 소비 → DB 저장, Spring Boot 멀티 모듈 아키텍처 설계 (signal-api, signal-chat, signal-common, signal-media), Hexagonal Architecture 적용으로 Domain/Application/Infrastructure Layer 분리
- **결과**: 응답시간 **2.5ms 이하** 달성, **초당 5,000+ 메시지** 처리 가능한 확장성 확보, MSA 전환 대비 모듈별 독립 배포 구조 구축, 공통 도메인 재사용으로 코드 중복 **30% 감소**
- **기술 스택**: RabbitMQ STOMP, Spring Boot Multi-Module, Hexagonal Architecture, Gradle

---

#### 📈 **성능 테스트 자동화 및 정량적 데이터 기반 기술 의사결정**

- **문제**: 성능 개선 효과를 정량적으로 검증할 방법 부재, 수동 테스트로는 대규모 동시 접속 환경 재현 불가, 병목 지점 파악 어려움
- **해결**: Python 기반 부하 테스트 스크립트 작성 (asyncio + websockets, 최대 500명 동시 접속 시뮬레이션), 성능 지표 측정 자동화 (Throughput, Latency P50/P95/P99, 순서 정확도), Redis seqId vs DB AUTO_INCREMENT 비교 테스트 수행
- **결과**: 10,000건 메시지 동시성 테스트 성공, Redis 방식이 처리량 60% 향상·지연시간 50% 감소 검증, 정량적 데이터 기반 기술 스택 선정 정당화
- **기술 스택**: Python, asyncio, websockets

---

#### 🔍 **Elasticsearch 기반 대용량 메시지 전문 검색 시스템 구축**

- **문제**: PostgreSQL LIKE 쿼리로는 수백만 건 메시지 검색 시 성능 저하 (응답 10초 이상), 한국어 형태소 분석 부재로 검색 정확도 낮음, 검색 결과 랭킹 알고리즘 부재, 실시간 메시지 인덱싱 지연
- **해결**: Elasticsearch 클러스터 구축 및 Logstash로 PostgreSQL → Elasticsearch 실시간 동기화, Nori 형태소 분석기로 한국어 검색 품질 개선 (조사 분리, 어간 추출), BM25 랭킹 알고리즘으로 검색 결과 정렬, Highlight API로 검색어 강조, Suggest API로 자동완성 구현
- **결과**: 100만 건 이상 메시지에서 검색 응답시간 100ms 이하 달성, 형태소 분석으로 검색 정확도 40% 향상, 자동완성으로 사용자 검색 편의성 개선, 실시간 인덱싱으로 3초 이내 검색 가능
- **기술 스택**: Elasticsearch 8.x, Logstash, Nori Analyzer, Spring Data Elasticsearch, BM25 Algorithm

---

#### 🔔 **Redis 기반 읽지 않은 메시지 실시간 동기화 시스템**

- **문제**: 채널별 읽지 않은 메시지 개수 조회 시 매번 DB 쿼리로 인한 부하 (평균 200ms), 여러 디바이스에서 동시 접속 시 동기화 지연, 대량의 채널 구독 시 초기 로딩 시간 증가 (100개 채널 시 20초 이상), N+1 쿼리 문제
- **해결**: Redis Sorted Set으로 사용자별 채널의 마지막 읽은 메시지 seqId 저장 (`key: user:{userId}:read, score: seqId, member: channelId`), Redis Pub/Sub으로 읽음 상태 변경 시 모든 디바이스에 실시간 브로드캐스트, Pipeline 배치 조회로 N+1 쿼리 해결, Redis TTL로 메모리 효율 관리
- **결과**: 읽지 않은 메시지 조회 응답시간 95% 감소 (200ms→10ms), 멀티 디바이스 동기화 지연 1초 이내, 100개 채널 초기 로딩 3초 이내, DB 부하 80% 감소
- **기술 스택**: Redis Sorted Set, Redis Pub/Sub, Redis Pipeline, Spring Data Redis

---

#### 🛡️ **Spring Security 기반 계층적 권한 관리 시스템 (RBAC) 구현**

- **문제**: 서버(길드)별 복잡한 권한 구조 (Owner, Admin, Moderator, Member), 채널별 읽기/쓰기 권한 세분화 필요, 역할 계층 구조 관리 어려움, 권한 변경 시 실시간 반영 필요, 매 요청마다 DB 조회로 성능 저하
- **해결**: Role-Based Access Control (RBAC) 도메인 모델 설계 (Server-Role-Permission), Spring Security `@PreAuthorize`로 메서드 레벨 권한 검증, Custom `PermissionEvaluator`로 세분화된 권한 체크 로직 구현, Redis 기반 권한 캐싱으로 조회 성능 최적화 (TTL 5분), 권한 변경 시 Redis Cache Eviction + WebSocket으로 실시간 알림
- **결과**: 세분화된 권한 관리로 보안 강화, 권한 조회 응답시간 80% 감소 (100ms→20ms), 권한 변경 즉시 반영으로 일관성 보장, 채널별 읽기/쓰기 권한 독립 관리
- **기술 스택**: Spring Security, @PreAuthorize, Custom PermissionEvaluator, Redis Cache, WebSocket

---

#### ⚡ **Token Bucket Algorithm 기반 API Rate Limiting 구현**

- **문제**: 악의적인 사용자의 API 과다 호출로 서버 과부하 (메시지 스팸, 무한 요청), DDoS 공격 위험, 일반 사용자 서비스 품질 저하, 특정 엔드포인트에 트래픽 집중
- **해결**: Redis 기반 Token Bucket Algorithm 구현 (Lua Script로 원자성 보장), 사용자별/IP별 요청 제한 (메시지 전송: 10건/초, API 호출: 100건/분), Spring Interceptor로 요청 전 검증, 제한 초과 시 `429 Too Many Requests` + Retry-After 헤더 응답, Custom Annotation `@RateLimit`으로 선언적 적용
- **결과**: DDoS 공격 방어 및 스팸 메시지 차단, 서버 안정성 향상으로 99.9% 가용성 유지, 일반 사용자 서비스 품질 보장, 엔드포인트별 독립적 Rate Limit 설정
- **기술 스택**: Redis Lua Script, Token Bucket Algorithm, Spring Interceptor, Custom Annotation

---

### 2. [생각등대 부트캠프] 교육 커뮤니티 서비스 개발

**일정**: 2025.02 ~ 2024.05 (3인 프로젝트)  
**기술 스택**: Spring Boot 3.2, MySQL 8.0, Java 17, JPA, MyBatis, JSP, Thymeleaf, Redis, AWS

**주요 성과**:

- **응답 속도 개선**: Redis 캐시 적용을 통해 리스트 조회 성능 향상 (350ms → 40ms)
- **데이터 정합성 확보**: @Transactional 적용 및 계층 분리로 트랜잭션 처리 안정화
- **쿼리 최적화**: DB Index 튜닝 및 정렬 로직 개선으로 속도 향상 (2s → 150ms)
- **기능 구현**: OAuth2 애플 로그인, SMTP 관리자 메일 발송 기능 구현
- **환경 구축**: Docker 기반 TestContainer 활용 테스트 환경 및 AWS EC2/S3 배포 파이프라인 구축

---

## 🎓 Education & Experience

### SSAFY 12기 (삼성 청년 SW 아카데미)

**기간**: 2024.07 ~ 2025.06 (예정)  
**전공**: Java 웹 백엔드 트랙

- Spring Boot, JPA, Redis, RabbitMQ 등 실무 기술 스택 학습
- 팀 프로젝트를 통한 협업 및 문제 해결 경험

---

### 경희대학교

**기간**: 2018.03 ~ 2024.06  
**전공**: 물리학과

---

### 생각등대 컴퍼니 인턴

**기간**: 2020.07 ~ 2020.09  
**기술 스택**: React, TypeScript, TypeORM

- React, TypeScript, TypeORM 등 활용 실 운영 서버 적용 프로젝트 진행
- 새로운 기술을 채택하는 과정에서, 이론 학습보다 실제 프로젝트에 적용하는 경험이 효과적임을 인지함

---

### 클린 API로 가는 길 3기

- RestAssured 기반 통합 테스트 및 테스트 전략 학습

---

## 💻 Skills

### Backend
- **Language**: Java 17
- **Framework**: Spring Boot 3.4, Spring Data JPA, QueryDSL, Spring Security
- **Database**: PostgreSQL, MySQL, Redis
- **Message Queue**: RabbitMQ
- **Search Engine**: Elasticsearch, Logstash
- **Test**: JUnit 5, Mockito, RestAssured

### DevOps & Tools
- **Container**: Docker, Docker Compose
- **VCS**: Git, GitHub
- **Build Tool**: Gradle
- **Cloud**: AWS (EC2, S3, CloudFront)

### Performance & Testing
- **Load Testing**: Python (asyncio, websockets)
- **Monitoring**: 성능 지표 측정 (Throughput, Latency, Order Accuracy)
- **Rate Limiting**: Token Bucket Algorithm, Redis Lua Script

---

## 📝 기술 블로그

- [Redis 기반 메시지 순서 보장 메커니즘 성능 분석](https://velog.io/@akrap)
- [RabbitMQ를 활용한 분산 채팅 시스템 설계](https://velog.io/@akrap)
- [Elasticsearch를 이용한 대용량 메시지 검색 시스템](https://velog.io/@akrap)
- [Spring Boot 멀티 모듈 아키텍처 설계 경험](https://velog.io/@akrap)

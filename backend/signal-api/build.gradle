dependencies {
    implementation project(':signal-common')
    
    // DB Driver
    runtimeOnly 'org.postgresql:postgresql'

    // Swagger
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.3'

    testImplementation(testFixtures(project(':signal-common')))
}
